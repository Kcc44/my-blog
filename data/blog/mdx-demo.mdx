---
title: MDX 语法功能演示
date: 2026-06-11
tags: ['技术', '博客', 'MDX']
summary: 包含所有 Markdown 和 MDX 功能的对照展示。
---

## 一、文字格式

```md
**粗体**  *斜体*  ~~删除线~~  `行内代码`
```

**粗体**  *斜体*  ~~删除线~~  `行内代码`

---

## 二、标题层级

```md
### 三级标题
#### 四级标题
```

---

## 三、链接

```md
[GitHub](https://github.com/Kcc44)
```

[GitHub](https://github.com/Kcc44)

---

## 四、图片

```md
![随机图片](https://picsum.photos/600/300)
```

![随机图片](https://picsum.photos/600/300)

---

## 五、数学公式

### 行内公式

```md
爱因斯坦质能方程 $E = mc^2$ 简洁优美。
```

爱因斯坦质能方程 $E = mc^2$ 简洁优美。

### 块级公式

```md
$$
\int_0^\infty e^{-x^2} dx = \frac{\sqrt{\pi}}{2}
$$
```

$$
\int_0^\infty e^{-x^2} dx = \frac{\sqrt{\pi}}{2}
$$

### 矩阵

```md
$$
\begin{pmatrix}
1 & 2 & 3 \\
4 & 5 & 6 \\
7 & 8 & 9
\end{pmatrix}
$$
```

$$
\begin{pmatrix}
1 & 2 & 3 \\
4 & 5 & 6 \\
7 & 8 & 9
\end{pmatrix}
$$

---

## 六、代码块（语法高亮）

### Python

````md
```python
def binary_search(arr, target):
    left, right = 0, len(arr) - 1
    while left <= right:
        mid = (left + right) // 2
        if arr[mid] == target:
            return mid
        elif arr[mid] < target:
            left = mid + 1
        else:
            right = mid - 1
    return -1
```
````

```python
def binary_search(arr, target):
    left, right = 0, len(arr) - 1
    while left <= right:
        mid = (left + right) // 2
        if arr[mid] == target:
            return mid
        elif arr[mid] < target:
            left = mid + 1
        else:
            right = mid - 1
    return -1
```

### C++

```cpp
#include <iostream>
using namespace std;

int main() {
    cout << "Hello, World!" << endl;
    return 0;
}
```

---

## 七、表格

```md
| 算法 | 时间复杂度 | 空间复杂度 |
|------|-----------|-----------|
| 二分查找 | O(log n) | O(1) |
| 快速排序 | O(n log n) | O(log n) |
| 动态规划 | O(n²) | O(n) |
```

| 算法 | 时间复杂度 | 空间复杂度 |
|------|-----------|-----------|
| 二分查找 | O(log n) | O(1) |
| 快速排序 | O(n log n) | O(log n) |
| 动态规划 | O(n²) | O(n) |

---

## 八、列表

### 无序列表

```md
- 算法与数据结构
- 项目开发经验
- 论文阅读笔记
```

- 算法与数据结构
- 项目开发经验
- 论文阅读笔记

### 有序列表

```md
1. 打开编辑器
2. 编写代码
3. 提交到 GitHub
```

1. 打开编辑器
2. 编写代码
3. 提交到 GitHub

### 任务列表

```md
- [x] 搭建博客
- [x] 配置评论
- [ ] 发布文章
```

- [x] 搭建博客
- [x] 配置评论
- [ ] 发布文章

---

## 九、引用

```md
> 代码是写给人看的，顺便给机器运行。
> —— *Structure and Interpretation of Computer Programs*
```

> 代码是写给人看的，顺便给机器运行。
> —— *Structure and Interpretation of Computer Programs*

---

## 十、提示框

```md
> [!NOTE]
> 这是一个提示信息，用于补充说明。

> [!WARNING]
> 这是一个警告信息，提醒需要特别注意的内容。

> [!IMPORTANT]
> 重要信息，你应当仔细阅读。
```

> [!NOTE]
> 这是一个提示信息，用于补充说明。

> [!WARNING]
> 这是一个警告信息，提醒需要特别注意的内容。

> [!IMPORTANT]
> 重要信息，你应当仔细阅读。

---

## 十一、分隔线

```md
---
```

---

## 十二、视频（HTML 内嵌）

```html
<video controls width="100%">
  <source src="https://www.w3schools.com/html/mov_bbb.mp4" type="video/mp4" />
  你的浏览器不支持视频标签。
</video>
```

<video controls width="100%">
  <source src="https://www.w3schools.com/html/mov_bbb.mp4" type="video/mp4" />
  你的浏览器不支持视频标签。
</video>

---

## 十三、折叠面板

```html
<details>
<summary>点击展开更多内容</summary>

这里是被折叠的内容。可以是任何 Markdown 或 HTML。

- 条目一
- 条目二
</details>
```

<details>
<summary>点击展开更多内容</summary>

这里是被折叠的内容。可以是任何 Markdown 或 HTML。

- 条目一
- 条目二
</details>

---

## 总结

以上所有内容都写在 **一个 `.mdx` 文件** 里，放在 `data/blog/` 目录下即可自动发布。
